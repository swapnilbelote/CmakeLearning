- Create one folder in this directory, "Build".
- "cmake ../" command in the directory created.
- After that run "make"
- To install the binary, "make install"
- See the "basic" executable in Build/bin folder
